{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": ["test.txt"],
            "cwd": "${fileDirname}",
            "internalConsoleOptions": "neverOpen",
            "preLaunchTask": "Build"
        }
    ]
}